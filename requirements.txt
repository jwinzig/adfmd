# Runtime: adfmd has no third-party dependencies (Python stdlib only).
# Source of truth is pyproject.toml. This file installs the package with
# development extras for local work and CI:
#   pip install -e ".[dev]"
-e ".[dev]"
